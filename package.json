{
  "name": "telegram-mini-shop",
  "version": "1.0.0",
  "type": "module",
  "engines": { "node": ">=18" },

  "scripts": {
    // back-end
    "start": "node index.js",

    // front-end
    "dev:web": "cd webapp && vite",
    "build:web": "cd webapp && vite build",

    // общий билд для Render
    "render-build": "yarn build:web"
  },

  "dependencies": {
    "express": "^4.19.2",
    "node-telegram-bot-api": "^0.61.2"
  },
  "devDependencies": {
    "vite": "^5.2.0",
    "@vitejs/plugin-react": "^4.0.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "classnames": "^2.5.1",
    "autoprefixer": "^10.4.17",
    "postcss": "^8.4.34",
    "tailwindcss": "^3.4.4"
  }
}
